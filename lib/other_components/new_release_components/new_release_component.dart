

import 'package:flutter/material.dart';

class NewReleaseScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("New Release Movies Screen"),
      ),
    );
  }

}